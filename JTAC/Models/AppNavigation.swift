import Foundation

enum AppScreen {
    case main
    case liveTranscript
    case nineLine
    case map
}

enum SectionType: String {
    case liveTranscript = "Live Radio Transcript"
    case nineLine = "9 Line"
    case map = "Map View"
}
